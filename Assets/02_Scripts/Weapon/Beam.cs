using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Beam : MonoBehaviour
{
    
    // sinusoidal function is a function using the sine function.
    // The basic form of a sinusoidal function is y = A sin (B(x - C)) + D,
    // where A is the amplitude or height of our function, B is the change in
    // period defined by 2pi/B, C the horizontal shift, and D the vertical shift.
    
    
    
    
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
